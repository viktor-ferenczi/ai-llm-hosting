#!/bin/bash
set -euo pipefail

SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
. "$SCRIPT_DIR/llm-config"

. ~/env/vllm-0.2.7/bin/activate

python -O -u -m $LLM_API_SERVER_MODULE --help
